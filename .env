# Local development — replace with your local Postgres URL or Vercel Postgres URL
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL="postgresql://localhost:5432/trace_dev"

# Anthropic API — required for Generate Production AI Statement feature
# Add your key here for local dev, and to Vercel project env vars for production
ANTHROPIC_API_KEY=""
